/target
**/*.rs.bk
*.pdb

# Regenerated by cbindgen on every build
crates/resilum-ffi/resilum.h

# Keep Cargo.lock committed (workspace has binaries)

keys/
*.pem
*.key
secrets.*
.codeberg-token

# Per-node runtime state under the deploy/config bind mount.
deploy/config/**
!deploy/config/**/
!deploy/config/*.example
!deploy/config/**/*.example

deploy/compose.override.yaml

# Per-checkout: local [patch] and registry overrides
.cargo/

docs/

# Leak patterns are project-private by definition
lints/private-patterns.txt
