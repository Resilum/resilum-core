---
extends: default

ignore: |
  target/
  deploy/compose.override.yaml

rules:
  # Commented-out config examples must sit at the indentation of the code they
  # stand in for, which this rule flags against the surrounding content.
  comments-indentation: disable
