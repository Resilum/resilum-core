[repositories]

# Bare repositories under this directory are served over Reticulum.
# Drop `<name>.git` dirs in, or run `rngit mirror <upstream>
# rns://<self>/public/<name>` to populate.
public = /config/rngit/repos/public

[access]

public = r:all
