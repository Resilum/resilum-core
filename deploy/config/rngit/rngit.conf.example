[repositories]

# `rngit mirror <upstream> rns://<self>/mirrors/<name>` creates a bare
# copy of `<upstream>` here and serves it as `rns://<self>/mirrors/<name>`.
mirrors = /config/rngit/repos/mirrors

[access]

mirrors = r:all
