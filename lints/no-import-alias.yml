---
id: no-import-alias
language: Rust
severity: error
message: Renamed import; import the real name or qualify with the module.
note: |
  `use a::b::C as D` hides where `D` came from. Import `C` directly, or drop the
  `use` and write `b::C` at the call site. Only `as _` is allowed.
rule:
  kind: use_as_clause
  not:
    has:
      field: alias
      regex: ^_$
